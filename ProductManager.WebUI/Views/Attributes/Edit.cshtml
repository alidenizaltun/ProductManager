@model ProductManager.WebUI.Models.ProductOperations.AttributeDefinitionFormViewModel

@{
    var isEdit = Model.Id.HasValue;
    ViewData["Title"] = isEdit ? "Özellik Düzenle" : "Yeni Özellik";
}

<div class="nk-content nk-content-fluid pm-form-page">
    <div class="container-fluid">
        <div class="nk-content-inner">
            <div class="nk-content-body">
                <partial name="_StatusMessage" />

                <div class="nk-block-head nk-block-head-sm">
                    <div class="nk-block-between">
                        <div class="nk-block-head-content">
                            <h3 class="nk-block-title page-title">@(isEdit ? "Özellik Düzenle" : "Yeni Özellik")</h3>
                            <div class="nk-block-des text-soft">
                                <p>Veri tipi ve zorunluluk kurallarıyla özellik tanımlarını yönetin.</p>
                            </div>
                        </div>
                        <div class="nk-block-head-content">
                            <a asp-action="Index" class="btn btn-outline-light bg-white"><em class="icon ni ni-arrow-left"></em><span>Listeye Dön</span></a>
                        </div>
                    </div>
                </div>

                <div class="nk-block">
                <form method="post" asp-action="@(isEdit ? "Edit" : "Create")">
                    @Html.AntiForgeryToken()
                    <input asp-for="Id" type="hidden" />

                    <div class="card card-bordered pm-form-card">
                        <div class="card-inner border-bottom py-2">
                            <h6 class="title mb-0">Özellik Bilgileri</h6>
                        </div>
                        <div class="card-inner">
                            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                            <div class="row g-gs">
                                <div class="col-md-4">
                                    <label asp-for="Key" class="form-label"></label>
                                    <input asp-for="Key" class="form-control" />
                                    <span asp-validation-for="Key" class="text-danger"></span>
                                </div>
                                <div class="col-md-8">
                                    <label asp-for="DisplayName" class="form-label"></label>
                                    <input asp-for="DisplayName" class="form-control" />
                                    <span asp-validation-for="DisplayName" class="text-danger"></span>
                                </div>
                                <div class="col-md-3">
                                    <label asp-for="DataType" class="form-label"></label>
                                    <input asp-for="DataType" class="form-control" type="number" min="1" />
                                </div>
                                <div class="col-md-3 d-flex align-items-center">
                                    <div class="form-check mt-4">
                                        <input asp-for="IsRequired" class="form-check-input" />
                                        <label asp-for="IsRequired" class="form-check-label"></label>
                                    </div>
                                </div>
                                <div class="col-md-3 d-flex align-items-center">
                                    <div class="form-check mt-4">
                                        <input asp-for="IsFilterable" class="form-check-input" />
                                        <label asp-for="IsFilterable" class="form-check-label"></label>
                                    </div>
                                </div>
                                <div class="col-md-3 d-flex align-items-center">
                                    <div class="form-check mt-4">
                                        <input asp-for="IsVariantAxis" class="form-check-input" />
                                        <label asp-for="IsVariantAxis" class="form-check-label"></label>
                                    </div>
                                </div>
                                <div class="col-12">
                                    <label asp-for="AllowedValuesJson" class="form-label"></label>
                                    <textarea asp-for="AllowedValuesJson" class="form-control" rows="2"></textarea>
                                </div>
                                <div class="col-12">
                                    <label asp-for="ValidationRuleJson" class="form-label"></label>
                                    <textarea asp-for="ValidationRuleJson" class="form-control" rows="2"></textarea>
                                </div>
                            </div>

                            <div class="mt-4 pt-3 border-top d-flex flex-wrap justify-content-end gap-2">
                                <button type="submit" class="btn btn-primary">Kaydet</button>
                                <a asp-action="Index" class="btn btn-outline-light">Listeye Dön</a>
                            </div>
                        </div>
                    </div>
                </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
