@model ProductManager.WebUI.Models.ProductOperations.SupplierFormViewModel

@{
    var isEdit = Model.Id.HasValue;
    ViewData["Title"] = isEdit ? "Tedarikçi Düzenle" : "Yeni Tedarikçi";
}

<div class="nk-content">
    <div class="container-fluid">
        <div class="nk-content-inner">
            <div class="nk-content-body">
                <div class="nk-block-head nk-block-head-sm">
                    <div class="nk-block-between">
                        <div class="nk-block-head-content">
                            <h3 class="nk-block-title page-title">@(isEdit ? "Tedarikçi Düzenle" : "Yeni Tedarikçi")</h3>
                            <div class="nk-block-des text-soft">
                                <p>Tedarikçi kimlik, iletişim ve durum bilgilerini güncelleyin.</p>
                            </div>
                        </div>
                        <div class="nk-block-head-content">
                            <a asp-action="Index" class="btn btn-outline-light">Listeye Dön</a>
                        </div>
                    </div>
                </div>

                @await Html.PartialAsync("_StatusMessage")

                <div class="nk-block">
                    <div class="card card-bordered">
                        <div class="card-inner">
                            <div class="card-head">
                                <h5 class="card-title">Tedarikçi Bilgileri</h5>
                            </div>

                            <form method="post" asp-action="@(isEdit ? "Edit" : "Create")" asp-route-id="@Model.Id" class="row g-4">
                                @Html.AntiForgeryToken()
                                <input asp-for="Id" type="hidden" />

                                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                                <div class="col-lg-4">
                                    <div class="form-group">
                                        <label asp-for="SupplierCode" class="form-label"></label>
                                        <div class="form-control-wrap">
                                            <input asp-for="SupplierCode" class="form-control" placeholder="Örn: TED-001" />
                                        </div>
                                        <span asp-validation-for="SupplierCode" class="text-danger"></span>
                                    </div>
                                </div>

                                <div class="col-lg-8">
                                    <div class="form-group">
                                        <label asp-for="Name" class="form-label"></label>
                                        <div class="form-control-wrap">
                                            <input asp-for="Name" class="form-control" placeholder="Tedarikçi adını girin" />
                                        </div>
                                        <span asp-validation-for="Name" class="text-danger"></span>
                                    </div>
                                </div>

                                <div class="col-lg-4">
                                    <div class="form-group">
                                        <label asp-for="TaxNumber" class="form-label"></label>
                                        <div class="form-control-wrap">
                                            <input asp-for="TaxNumber" class="form-control" placeholder="Vergi numarası" />
                                        </div>
                                        <span asp-validation-for="TaxNumber" class="text-danger"></span>
                                    </div>
                                </div>

                                <div class="col-lg-4">
                                    <div class="form-group">
                                        <label asp-for="Email" class="form-label"></label>
                                        <div class="form-control-wrap">
                                            <input asp-for="Email" class="form-control" placeholder="ornek@firma.com" />
                                        </div>
                                        <span asp-validation-for="Email" class="text-danger"></span>
                                    </div>
                                </div>

                                <div class="col-lg-4">
                                    <div class="form-group">
                                        <label asp-for="Phone" class="form-label"></label>
                                        <div class="form-control-wrap">
                                            <input asp-for="Phone" class="form-control" placeholder="Telefon" />
                                        </div>
                                        <span asp-validation-for="Phone" class="text-danger"></span>
                                    </div>
                                </div>

                                <div class="col-lg-12">
                                    <div class="form-group">
                                        <label asp-for="Address" class="form-label"></label>
                                        <div class="form-control-wrap">
                                            <textarea asp-for="Address" class="form-control" rows="3" placeholder="Adres bilgisi"></textarea>
                                        </div>
                                        <span asp-validation-for="Address" class="text-danger"></span>
                                    </div>
                                </div>

                                <div class="col-lg-12">
                                    <div class="form-group">
                                        <div class="custom-control custom-control-sm custom-checkbox">
                                            <input asp-for="IsActive" class="custom-control-input" />
                                            <label class="custom-control-label" for="@Html.IdFor(m => m.IsActive)">Aktif</label>
                                        </div>
                                    </div>
                                </div>

                                <div class="col-12">
                                    <div class="form-group">
                                        <button type="submit" class="btn btn-primary">
                                            <em class="icon ni ni-save"></em>
                                            <span>@(isEdit ? "Değişiklikleri Kaydet" : "Tedarikçiyi Kaydet")</span>
                                        </button>
                                        <a asp-action="Index" class="btn btn-outline-light">İptal</a>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
